// Placeholder global setup for the `integration` Vitest project.
// The real implementation (Testcontainers PostgreSQL with `DATABASE_URL`
// fallback for CI) is delivered in T10 (Phase 3). For now this is an
// intentional no-op so the `integration` project has a valid globalSetup
// entry point to extend later.
export default function setup() {}
